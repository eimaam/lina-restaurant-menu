# Assets Directory

Place your custom logo file here as:
`logo.png`

The system is already configured to automatically display `/src/assets/logo.png` across the header, footer, admin portal, and table QR stands, with a fallback luxury typography crest if not present.
